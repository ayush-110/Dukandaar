# Dukandaar

An e-commerce SaaS platform (inspired by Dukaan), built with FastAPI + React/Next.js — a learn-by-doing project evolving from a single-server MVP to a scaled, multi-tenant production system.

## Setup

uv sync      # install dependencies
uv run main.py   # run